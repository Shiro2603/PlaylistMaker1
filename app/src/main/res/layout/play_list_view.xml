<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/playListPicture"
        android:layout_width="160dp"
        android:layout_height="160dp"
        app:layout_constraintEnd_toEndOf="parent"
        android:src="@drawable/add_photo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


    <TextView
        android:id="@+id/playListName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="@+id/playListPicture"
        app:layout_constraintStart_toStartOf="@+id/playListPicture"
        app:layout_constraintTop_toBottomOf="@+id/playListPicture"
        android:textColor="@color/black"
        tools:text="Имя плейлиста"
        android:layout_marginTop="4dp"/>

    <TextView
        android:id="@+id/playListCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="@+id/playListPicture"
        app:layout_constraintStart_toStartOf="@+id/playListPicture"
        app:layout_constraintTop_toBottomOf="@id/playListName"
        tools:text="100 песен"
        android:textColor="@color/black"/>



</LinearLayout>